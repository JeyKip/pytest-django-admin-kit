[pytest]
# `pythonpath` puts `tests/` on sys.path so the test project imports as `project`,
# the way it would in a real repository. Requires pytest 7.0, which is the floor.
pythonpath = tests
DJANGO_SETTINGS_MODULE = project.settings
testpaths = tests
addopts = -ra
